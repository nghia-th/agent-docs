# Prompt — Update Existing Feature

Trace the existing feature across UI → API → service/domain → database → tests.

Do not assume the current implementation is correct. Identify the exact behavior that must change, compatibility impact, affected tests, and whether a migration is required.

Implement the smallest safe change and verify all affected layers.
