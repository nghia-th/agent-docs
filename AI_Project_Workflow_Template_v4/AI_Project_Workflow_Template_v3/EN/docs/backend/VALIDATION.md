# Backend Validation

Document validation at API and domain boundaries.

Cover:
- required fields
- length/range
- format
- enum/status
- cross-field rules
- ownership-dependent rules
- duplicate/unique conflicts
- database constraint conflicts

Validation errors must map to the project's standard error contract.
