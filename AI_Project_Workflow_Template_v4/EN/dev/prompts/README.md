# Development Prompts

These prompts are workflow helpers. They instruct the AI to inspect relevant `docs/`, feature, task, bug and decision records before editing.

Recommended sequence:

`feature-plan` → feature docs → `task` → implementation → `verification`
