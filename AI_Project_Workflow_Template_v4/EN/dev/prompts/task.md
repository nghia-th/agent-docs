# Prompt — Execute a Task

Read `AGENTS.md` and the referenced feature/bug/decision documents.

Before editing:
- inspect existing implementation
- identify exact files/modules
- verify API/data/security assumptions
- state a concise implementation plan

Then implement only the task scope.

After editing:
- run targeted tests
- run build/typecheck/lint when available
- verify affected API/database/security behavior
- update task status and relevant documentation if required
- report exact verification results
